use std::collections::HashMap;

fn main() {
//    let args: Vec<String> = env::args().collect();
//    let str: &str = args[0];
//    println!(str);
    let size: f64 = 16.0;

    return size.sqrt() / 2.0 * std::f64::consts::PI
}
